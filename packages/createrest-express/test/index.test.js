import test from 'ava'
import { createRest } from 'createrest'
import { createExpressMiddleware } from '../lib'

test.todo('Write real async tests')

// test('Fake test', t => {
//   createRestExpress(createRest(r => {
//     r.get(() => {})
//   }))
//   t.pass()
// })
